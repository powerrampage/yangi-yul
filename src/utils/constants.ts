export const sizeOptions = [10, 20, 50, 100].map((c) => ({
	label: c?.toString(),
	value: c?.toString()
}));

export const DEFAULT_PAGE_SIZE = 10;

export const DATE_FORMAT = "DD.MM.YYYY";
export const DATE_FORMAT2 = "YYYY-MM-DD";
