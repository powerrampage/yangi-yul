import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#4A71FB",
    controlHeightLG: 48,
    controlHeight: 40,
    controlHeightSM: 32,
    controlHeightXS: 30,
  },
};

export default theme;
